$('document').ready(function() {
  if ($('.index').length) {
    $('.create-btn').click(function() {0
      console.log(0);
      $('.new_category').addClass('form-inline');
      $('.cat-form').removeClass('hidden');
    });
  }
});
